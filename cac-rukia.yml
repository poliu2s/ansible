---
- hosts: rukia
  user: root
  sudo: yes

  vars_files:
  - ./secrets/production.yml

  vars:
    hostname: rukia
    cloud: true
    pwd: /root

  roles:
  - wordpress

  tasks:
  - name: reboot the server
    command: reboot
    when: isFirstRun