---
- hosts: mikasa
  user: root
  sudo: yes

  vars_files:
  - ./secrets/vars/production.yml

  vars:
    hostname: mikasa
    cloud: true
    pwd: /root

  roles:
  - common

  tasks:
  - name: reboot the server
    command: reboot
    when: isFirstRun is defined